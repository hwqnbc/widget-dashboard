# End-to-end suites

Headless-Chromium tests that drive the real app and assert on the widgets'
public test contract — `data-testid` hooks and the `data-*` state widgets
publish. Most suites here cover the Drone Sim, Drone Strike and Tank Battle
widgets (whose HUD publishes telemetry every 150 ms), but the harness is
app-generic: any widget can get suites the same way (e.g. `120-avatars` drives
the Avatar Actions widget's `data-avatar`/`data-playing` contract, no WebGL
involved). For the WebGL games, flying/driving is **closed-loop**: a small
P-controller in `helpers.mjs` reads telemetry and steers the on-screen sticks
through CDP touch events (open-loop timed input is far too jittery to thread
gates or hit walls reliably).

```bash
npm run e2e            # all suites (starts its own dev server on :5199)
npm run e2e crash      # only suites whose filename matches "crash"
npm run e2e strike     # the Drone Strike suites (100-109, 117, 119, 123-129, 131-135)
npm run e2e tank       # the Tank Battle suites (110-116)
```

Environment:

- `CHROMIUM_PATH` — Chromium executable (default `/opt/pw-browsers/chromium`).
  Launched with `--enable-unsafe-swiftshader --use-angle=swiftshader` so WebGL
  works without a GPU.
- `E2E_PORT` — dev-server port (default 5199).

The runner bundles the widgets' pure modules (`flightModel`, `worldLayout`,
`lapTimer`, Drone Strike's `combatModel`/`waveLayout`, and Tank Battle's
`terrain`/`tankModel`/`shellModel`/`battleLayout`/`tankAI`, plus the Map
page's `routeGeometry`/`dragModel`/`basemapCatalog` and the app-wide
`utils/consoleLog` store, in later flat passes) with esbuild into
`e2e/.bundle/` so suites can compute waypoints and expected wave
compositions from the real (seeded, deterministic) layouts, and unit-check
pure app modules without a browser.
Screenshots land in `e2e/.artifacts/`. Both directories are gitignored.

Mode toggles live in the **settings panel** (gear button → dialog), not the
button row: suites flip them with `setSwitch(page, testId, desired)` from
`helpers.mjs` (opens the panel, syncs the switch, closes it) and read current
mode state from the widget root's `data-*` attributes via
`rootState(page, attr)`.

| Suite | Covers |
|---|---|
| `10-core` | element presence, climb + altitude hold, inertia braking, joystick recovery on window blur mid-drag and on silent pointer-capture loss (hasPointerCapture watchdog), simultaneous multi-touch, reset, camera cycle (tp/fp/los/walk) + persistence, grid-drag isolation |
| `15-chasecam` | chase-boom wall avoidance: full extension in open sky, clamps against a wall behind the drone (`data-boom`), re-extends in the clear |
| `16-op-avatar` | operator = Player 1's avatar: default seat map renders the toy's `Model3D` (`data-op-avatar`/`data-op-figure` on the root), swapping Player 1 → DarkArin/frak/Imperium/Gold Gunner/Scar/Bazooka Joe/Ninja on the real Settings page renders each avatar's model (all nine carry one — `data-op-figure` is always `avatar`; the basic primitive survives only as the Suspense fallback), swapping back restores toy; telemetry alive throughout |
| `17-walker` | walking pilot: follow on foot (speed hard-capped, drone outruns it, follow-band idle), configurable follow distance, pilot chip (standing/walking/holding/auto-rescue/manual-walk), hold toggle + resume, manual FPS walk while the drone is down (turn + walk-along-facing via sticks), full battery rescue (drain to dead → retrieve → carry → place on pad → recharge revives) via `data-op-*` |
| `18-input` | keyboard (W/S/A/D + arrows) and gamepad (stubbed `navigator.getGamepads`, scripted axes) drive the shared ControlInput; `data-input-source` arbitration, deadzone, zero-once release, typing-in-Notes guard, touch still works beside an idle pad |
| `19-fpv` | FPV polish (default off): horizon overlay only in fp + toggle on, banks with strafe (`data-roll`), levels on release, persistence |
| `20-collision` | closed-loop rooftop landing: rests on the roof, stays in the footprint, no tunneling under sustained descent |
| `30-timetrial` | full lap (pad → gates 1-3 → pad): timer start/finish, TO PAD phase, banner, laps/best bookkeeping, mid-lap reset, persistence, ghost render |
| `40-shuffle` | new-course button: instant shuffle vs ConfirmDialog guard, stat clearing, seed persistence |
| `45-gates` | gate-count slider: 3→6 on the same world, HUD/minimap follow, persistence, gate 1/6 sequences, mid-lap confirm guard + stat clearing (incl. the settings-reset path) |
| `46-editor` | course editor: fly & drop gates (pad/stacked-drop validation, undo), save → custom course races a full lap, persistence, seeded↔custom switching with confirm guard |
| `50-weather` | storm toggle: hands-off wind drift vs clear station-hold, HUD wind readout, persistence |
| `60-crash` | crash mode: full-speed wall hit → tumble + banner + lap void + pad respawn; safe mode → wall pin; toggle persistence |
| `70-haptics` | vibration recorder stub: contact buzz + cooldown, gate/crash patterns, no-API degradation |
| `75-sound` | AudioContext recorder stub: off by default (no context constructed), enable builds the graph + starts the twin sawtooth rotor oscs, stick effort pitches the hum above the 85 Hz idle, gate chime, crash thud + rotor muted while tumbling, suspend on disable, toggle persistence |
| `76-craft` | Fly as Lloyd (craft swap): drone by default, toggle flips `data-craft`, flight identical as Lloyd (climb, altitude hold, lap start), persistence across reload, Reset settings restores the drone; tp screenshot artifact for pose/scale eyeballing |
| `80-acro` | flight-mode toggle: hold brakes vs acro coasts, gravity fall beats the descent cap, persistence |
| `85-tuning` | rates/expo panel: speed/yaw sliders scale HUD-observable flight, turbo stacks, persistence, reset-to-defaults restores tuning + toggles |
| `90-minimap` | inset map: layout contents, marker tracks position/heading, toggle + persistence |
| `95-richworld` | scenery toggle contract + persistence (generation is unit-covered) |
| `97-landing` | landing challenge: pad markers, scored touchdown + banner/best, plain-roof no-score, persistence |
| `98-battery` | battery mode: bar, drain under effort, pad recharge, transient level, persistence |
| `100-strike-core` | Drone Strike: element presence + root defaults, damage-vignette + pad-chip at-rest contracts (`data-flash`/`data-low-hp`/`data-pad-state`), wave intro→active, seeded wave-1 composition vs the pure module (every kind can appear from wave 1 — static balloons, drifting ring-drones, moving SWAT car + military truck, a throttled non-firing enemy drone, a non-firing AA turret), nearest-target beacon (any kind), fire-button tap + cooldown-limited hold, closed-loop engagement (aim onto the beacon, fire, target down, score/hits) |
| `101-strike-waves` | wave progression: full wave-1 clear → cleared → wave 2 with the seeded target count; the wave model from the pure module — every kind from wave 1 (drifters, enemy drones with orbit envelopes), the easing is difficulty + the wave-scaled enemy throttle (`enemyAggressionScale` ramps 0.3→1, wave-1 enemies orbit slower than wave-5) + the return-fire gate (enemies/turrets hold fire on wave 1, armed by wave 5); best score/wave persistence across reload |
| `102-strike-input` | multi-touch (stick climbs while a second finger holds fire), keyboard W + Space with `data-input-source` arbitration, aim-assist/gyro-mode/auto-fire settings round-trips, hands-off auto-fire kill, progress-guarded restart |
| `103-strike-zoom` | ADS/zoom: scope-button toggle (`data-zoom` on root/HUD/reticle), scoped sensitivity + FOV scale by 1/power (`zoomSensFor`/`zoomFovFor` pure module — closed-loop yaw timing is FOV/framerate-confounded under software GL), firing while scoped, hold-Shift zoom, scoped assist cones tighter per level (pure module), gyro "Zoom only" mode, scope hidden + dropped outside FPV; adjustable zoom power (`data-zoom-power` default 2×, round-trip + persistence) |
| `104-strike-simports` | the sim-ported settings: hold brakes vs acro coasts (closed-loop), turbo ≈ 1.4× top speed, battery bar + effort drain + spawn-pad recharge + near-full transient restart, persistence of all three across reload |
| `105-strike-crash` | crash mode (default on): closed-loop full-speed wall ram → tumble + CRASHED! banner + one heart lost + pad respawn; pad rest restores the heart; safe-zone contract on the pad (`data-safe`, chip states, weapons offline) and clears on lift-off; safe-mode ram only pins; toggle persistence. Suites 100–102/104 disable crash mode after setup — their routes bump walls by design |
| `106-strike-mobile` | responsive touch layout on an iPhone-landscape viewport (844×390) in fullscreen: every stick/fire/weapon-chip/scope control fully on screen, the fire column (fire → chip → scope, bottom to top) inward of the right stick and clear of the toolbar, sizes scaled to the height, fire still works; `launch({viewport})` emulates the screen |
| `107-strike-gimbal` | four aim modes (Classic default = fly-to-aim, gimbal frozen; drag ignored, reticle centred), then in Reticle mode: drag-to-aim slews `data-gimbal-yaw/-pitch` (reticle moves in Reticle mode, centred in Gunner), deep ground look-down + arc clamp, double-tap recenter, gimbal+assist kill with flight sticks idle, hover mode re-routes the right stick (drone holds position), `trackToward` soft-track dynamics (pure module), mode persistence |
| `108-strike-difficulty` | enemy difficulty: Easy default + settings round-trip (Easy/Normal/Hard) + persistence; pure-module presets (orbit easy<normal<hard, gentler/shorter evade, one-hit enemies, delayed return fire) and buildWave threading difficulty into count/hp/fire-wave while keeping the difficulty-independent targets (balloons/drifters/ground) seeded identically |
| `109-strike-ground` | ground-target waves: pure-module military supply trucks (`ground`) and SWAT cars (`car`) are both **moving road vehicles from wave 1** — bound to a seeded road lane (fixed cross-coord ≈ a road `at` ±0.8), `driftSpeed ≠ 0`, wave-scaled travel velocity, `stepDrift` drives each along its axis at constant velocity holding the lane; on the deck (low y), one-hit, difficulty-independent count (both drawn before the enemy block, spread across lanes by a shared allocator). AA turrets (`turret`) also from wave 1 (static; they hold fire on wave 1 across every difficulty; hp + shared return-fire gate follow the difficulty preset). Trucks render as the `MilitaryTruck` model and cars as the `LegoSwatTruck`, both via the shared `ModelTargets` pool (`faceVelocity` + `animate`). DOM clears wave 1 closed-loop and confirms wave 2 fields the seeded target count (hit model is a normal sphere — covered by 100) |
| `110-tank-core` | Tank Battle: element presence + root defaults, seeded wave-1 composition vs the pure module, terrain grounding (live `data-alt` matches the bundled `heightAt`), throttle/turn driving, camera-independent hull, turret traverse lag + settle, fire-button shot + reload gating, ADS zoom toggle |
| `111-tank-combat` | closed-loop combat: no lock from spawn (terrain cover — the pilot must crest the ridge), two engage-and-kill runs clearing wave 1, wave 2 arrives with the seeded count + armed enemies, sky/ground ballistic-solution reticle contract, best score/wave persistence across reload, progress-guarded mode switch + cancel |
| `112-tank-modes` | Waves ↔ Roam toggle (direct without progress), roam garrison size + 5-HP pool, terrain roughness reshaping (pure-module amplitude check), settings round-trips on the root, minimap toggle, reset-to-defaults keeps mode/roughness/seed, mode + roughness persistence across reload |
| `113-tank-autoturn` | auto-turn hull (default on): stationary aiming never swings the hull, hull converges onto the camera heading under throttle alone, stick-X override, toggle off → hull ignores the camera, off persists across reload, settings reset restores it |
| `114-tank-help` | first-run "How to play" overlay: auto-opens on a fresh widget (`data-help-seen` off), Got-it dismiss persists the flag, no auto-open after reload, ? button reopens + Escape closes, battle state machine unblocked throughout. Other tank suites dismiss it via `addTankWidget` |
| `115-tank-safezone` | spawn safe zone (the strike pad, groundside): `data-safe` + pad-chip contract at spawn, weapons offline inside vs online outside, then the full closed-loop repair run — clear wave 1, bait a real wave-2 hit standing in the open, retreat to the pad (chip REPAIRING, enemies hold fire) and rest until the heart restores |
| `116-tank-mobile` | responsive touch layout on an iPhone-landscape viewport (844×390) in fullscreen: every stick/fire/scope control fully on screen, fire/scope column inward of the right stick and clear of the toolbar, sizes scaled to the height, fire works after driving clear of the safe zone |
| `117-strike-audio` | Drone Strike sound effects: AudioContext stubbed with a recorder; default `data-audio` on + per-effect `data-sfx-*` counters; firing bumps sfx-fire and reaches the gesture-unlocked engine (resume + oscillators), kills bump sfx-pop, wave-clear plays the sting; muting from settings freezes the counters + engine while the gun still fires; mute persists across reload; the widget still runs with no Web Audio API at all |
| `118-fullscreen-continuity` | full-screen preserves the live game: with a wave active, firing puts ref-held state on the clock (`data-shots`), then entering full screen keeps a single instance (`drone-strike-root`/`strike-canvas` count stays 1) and reparents the canvas into the MUI Dialog (not a fresh mount) with `data-shots` never dropping to 0, firing continues the same session, and exiting reparents back out of the dialog with shots + active wave intact — proving the reparented-portal fix (a remount would zero the shots counter) |
| `119-strike-soldiers` | patrolling soldier targets: pure-module `soldier` targets appear from wave 1 and **patrol** — the first ⌈count/2⌉ pace a building ROOFTOP (each `(x,z)` a building centre, `y = b.h+0.9`, half-beat kept ≤ the roof so they never walk off), the rest walk a free-roam beat on the open GROUND (`y≈0.9`, off any building, whole beat clear of the city — not road-bound); a wave fields both. Ground patrols walk a **route** — a diagonal **line** (any heading) or a circular **loop** (`routeKind`/`routeAngle`), validated clear of buildings over the whole span; rooftop pacers stay axis-aligned lines. All move via the seeded `stepDrift` soldier branch (writing true velocity) — asserted by stepping a diagonal-line soldier (moves both x AND z) and a loop soldier (constant radius while circling), plus a standing sentry that never moves. Legs actually swing (shared `characters/shared/legGait` rig) driven by ground speed via `AimPose.speed`. **Kneel-to-fire** stance (Bazooka Joe): the pure `characters/shared/kneelStance` `stepKneel` driver kneels when firing from a stationary plant, holds it briefly past the shot then stands, and stays upright when fired on the move (blends out with ground speed) — factor stays in [0,1]. The rocketeer also **halts to fire**: `soldierPlantHold` plants only the rocket variant (not SMG), only with a clear shot, only within the fire lead, and never shortens an existing plant; `stepDrift` with a non-zero `driftHold` offsets the patrol clock so movement resumes from where the plant froze it. hp follows difficulty (easy 1 / normal 2); count clamped ≤3 (SoldierTargets pool); they ride the shared return-fire gate (hold fire on wave 1 across every difficulty, armed by wave 5 on normal — via `stepTurret`). Weapon-matched **variants**: variant 0 (rocket / Bazooka Joe) or 1 (SMG / Scar), assigned by order; the variant's weapon tags its projectile's `visual` (`SOLDIER_ROCKET`→rocket / `SOLDIER_SMG`→bolt, proven through `spawnProjectile`), so one enemy pool renders as both. DOM confirms the app fields the seeded wave-1 count. Rendered from the Scar / Bazooka Joe avatar `Model3D`s via SoldierTargets (feet on surface + walk bob, body faces travel while walking / player while firing, aim + fire pose); the live wave-1 soldier is cleared by 100/101/109 |
| `120-avatars` | Avatar Actions widget: default selection, every catalogued avatar selectable in order + renders a figure svg, the Idle/Celebrate toggle plays/stops the celebration (`data-playing`, `data-action`), switching avatar mid-play resets to the static figure, per-widget selection persists across reload while play state resets |
| `121-avatars-3d` | Avatar Actions 2D/3D toggle + 3D action library: 2D default, switching to 3D lazy-loads three.js and renders a WebGL canvas for the avatars with a `Figure3D` (`data-view`, `data-figure3d`, `figure3d-stage`), the action toggle lists each model's `actions3d` moves (Walk pinned first on every avatar, then the specials — toy: Dance + 6 7 Show; ninja: Pump + Draw; fireninja: Fire Blade; darkarin: Twin Cross; frak: Blade Flurry; imperium: Claw Slash; goldgunner: Guns Blazing; scar: Breach & Clear + Sight & Fire; bazookajoe: Rocket Launch + Take Aim; lloyd: Sword Chop + Fly; jettrooper: Jet & Blast) and drives `data-action`/`data-playing` (all eleven avatars carry a 3D figure — the `figure3d-unavailable` placeholder path is scaffolding for future avatars, probed only by the toy block's negative check), tapping the figure toggles the turntable (`data-spin`, uniform across avatars, persisted), views restore on switch-back, chosen view + spin preference persist across reload |
| `122-settings-mobile` | Settings page on a phone-portrait viewport (390×844): both per-seat avatar picker groups wrap instead of overflowing (every button box inside the viewport width), the app-bar collapses its brand text + nav labels to icons, no horizontal page overflow, the widest button (Imperium Claw General) selects on tap and persists across reload, and the About build stamp shows `Build yymmdd-hh:mm` with a parseable `data-build-iso` |
| `123-strike-sparks` | muzzle flash + impact sparks: the pure `sparkModel` burst pool — a new pool is dormant, `spawnBurst` fills a contiguous block at the emit point fully lit, bursts are **deterministic** (no randomness — two pools given identical calls produce byte-identical velocity/colour arrays), `stepSparks` flies (position moves), falls (gravity drains vel.y) and fades (alpha 0→dormant) each particle with a clamped dt (a 5 s hitch advances 0.05 s), muzzle bursts die quicker than impact showers, and the burst ring wraps back onto block 0 without corrupting its neighbour. Live: `data-sparks` on the HUD (monotonic burst count) increments on a single fire tap (a muzzle flash needs no kill) and keeps climbing under held fire alongside `data-shots` |
| `124-strike-laser` | hitscan laser + heat meter + weapon picker: pure — `LASER` is `kind:'laser'` with a real fire tick (`cooldown > 0`, never per-frame), `WEAPON_SPECS`/`coerceWeapon` plumbing; heat on CombatState (`addHeat` per shot → clamps at 100 and trips the overheat latch exactly once; `stepHeat` cools, holds the latch until `HEAT_RESET` then fires 'ready' exactly once — hysteresis — and drains to 0; per-shot heating outpaces cooling so heat is a real constraint); `fireHitscan` resolves the whole origin→maxRange segment on the spawn frame (nearest alive target sphere at the exact surface point, dead targets skipped, ground plane stops it as a 'world' hit at y=0, a miss ends at maxRange with `hit:null`); `spawnLaserBeam` fills free slots and recycles the OLDEST when the ring is full. Live — default is `data-weapon="bolt"` with no heat bar; picking `strike-weapon-laser` flips the root attr and mounts `strike-heat`; holding Space fires on the tick (`data-shots`), plays the zap voice (`data-sfx-zap`), raises the heat fill's `data-level` and still muzzle-flashes (`data-sparks`); the pick + heat bar survive a reload |
| `125-strike-lob` | ballistic lob + trajectory arc: pure — `LOB` is `kind:'ballistic'` with real gravity, known to `WEAPON_SPECS`/`coerceWeapon`; a spawned shell stepped through the REAL integrator (`stepProjectiles`) arcs (rises past launch height) and gravity lands it in a 'world' ground hit at y≈0; the `sampleTrajectory` hint uses the same integration — bounded path, rises to an apex then drops monotonically, terminates clamped ON the ground, and a gravity-0 weapon samples a straight level ray. Live — `strike-weapon-lob` flips root `data-weapon`, the laser-only heat bar stays unmounted, held Space fires on the heavier 0.5 s cadence (shots in [2,5] over 1.2 s) as real projectiles (`data-sfx-fire`), and the pick persists across reload |
| `126-strike-crates` | rooftop supply crates: pure — wave 1 has no crate (clean tutorial), every wave from `CRATE_FROM_WAVE` (2) fields one on a REAL building rooftop ((x,z) a building centre, `top` its height), never sharing a roof with a soldier pacer, loot alternating `CRATE_ROTATION` (`wave % 2` — non-weapon drops ONLY: bonus heart on even waves, score cache on odd; weapon drops died with the in-game weapon chip), and a crate is NOT a shootable target (no `crate` TargetKind — it lives beside the target list on `WaveSpec`); `resolveCrateGrant` — a heart is ALWAYS +1 (uncapped: at full hearts it stacks on as an overheal, never converts or evaporates), a cache always pays `CRATE_SCORE`; `milestoneHearts` — every `MILESTONE_SCORE` (500) session points pays a bonus heart (one crossing pays 1, a double-jump pays 2, sub-threshold pays 0, and a run reset re-syncs the paid line downward paying nothing; live: `data-milestones` starts 0); `crateReached` pickup detection — reached hovering on the disc and at the roof lip, not reached outside `CRATE_RADIUS`, at ground level under the roof, or above `CRATE_PICKUP_HEIGHT`. Live — wave 1 publishes `data-crate-active="no"` with the default bolt equipped; clearing wave 1 (pilot recipe) brings wave 2's crate online: `data-crate-active="yes"` with the exact seeded coords + loot on the HUD beacon (`data-crate-x/-z/-loot`), while `data-weapon` stays the settings pick until touched |
| `127-strike-shotgun` | shotgun fan: pure — `SHOTGUN` fans >1 pellets with a real spread, heavier pump cadence and shorter range than the bolt, known to the picker plumbing; `pelletDir` — pellet 0 flies exactly true, every pellet is a unit direction inside the spread cone, ring pellets pairwise distinct, the fan is deterministic (same index → identical direction), and straight-up aim falls back to a valid basis. Live — `strike-weapon-shotgun` flips root `data-weapon` (persists across reload), the laser-only heat bar stays unmounted, and one fire tap counts exactly ONE shot (`data-shots` +1) while putting a multi-pellet salvo in flight (`data-proj` ≥ 2) |
| `128-strike-homing` | homing missiles: pure — `HOMING` steers (`homing` rad/s > 0), wears the rocket visual, slower + heavier-cadenced than the bolt; `spawnProjectile` stores the fired-under lock as `targetIdx` (default −1 = flies dumb); stepped through the REAL integrator, a locked missile CURVES onto an off-axis target a straight shot provably misses (the identical unlocked shot flies past and despawns), one frame turns at most `homing·dt` at constant speed, and a dead target releases the lock. Live — `strike-weapon-homing` flips root `data-weapon` (persists across reload), the heat bar stays laser-only, and a fire tap launches a slow long-lived missile (`data-proj`). |
| `129-strike-weapon-chip` | in-game weapon selector: the chip mounts between fire and scope agreeing with the root's `data-weapon` (default bolt); a one-notch up-swipe (CDP touch via `swipeChip`) scrolls to the next gun, a long swipe scrolls multiple notches, a down-notch scrolls back, and the selection WRAPS at both ends; a clean tap cycles next (the tap decision lives in the window capture-phase pointerup fallback — it fires before the element handler and clears the pointer id); mouse-wheel over the chip steps down/up; keyboard `1`/`4` direct-select bolt/shotgun; the switched gun still fires (`data-shots`); and the pick persists across reload (the chip writes the SAME persisted `weapon` field as the settings picker) |
| `130-map` | Map page: stale-chunk recovery (aborted lazy-chunk URL → one auto-reload → boundary Reload card → recovers when reachable, isolated context; the latch is matched by its `chunk-reload:map:` prefix — `142-chunk-recovery` owns the full policy), no arcgis code on the dashboard, the lazy chunk loads via the nav link, view readiness via `data-map-status` (from `view.when()`, never networkidle), basemap + injected ArcGIS CSS follow the theme toggle (render-computed `data-basemap`, asserts offline), basemap gallery (12 panel tiles, explicit pick beats the theme toggle, CARTO `WebTileLayer` construction, reload persistence, back to Auto; pure `basemapCatalog` resolver + `probeTileUrls`/`nextBasemapFallback` units), basemap health watchdog (offline: every tile provider abort-routed → `data-basemap-health` reaches fallback/failed with the warning banner while `data-basemap` stays choice-derived; online: health ok, no banner, active id matches), 2D/3D toggle + reload persistence with a destroyed-map guard (the suite fails on ArcGIS's "map is already destroyed" warning — lesson #124's detach-before-destroy regression), overlays panel (right-edge slide-out, `data-panel`, hosts the basemap gallery and the 3D buildings + trees switches — 3D-only, independent, persisted `data-buildings`/`data-trees` — plus pins/drawings layer visibility), drawing into named overlay groups (SketchViewModel markers + polygons land in the active overlay; group add/activate/rename/hide/delete + persistence assert offline via `data-overlays`/`data-active-overlay`/per-row attrs, the draw-and-group flow — counts per group, visibility join via `data-visible-drawings`, per-shape delete, edit-in-place (select a marker in Edit mode, drag it, commit → its panel-label coordinates change), group delete removing its shapes — in the interactive branch), tool strip contract, pure `routeGeometry` + `dragModel` + `flightPathModel` unit checks (bundled: insert index, nearest-distance, scale-derived tap threshold, drag commit-race orderings incl. pointer-up outracing drag end, 3D flight-path lengths/sampling/heading/done semantics), pure `flightPlanModel` planner units (OSM height estimation, segment×polygon intervals, ring inflation, direct/climb/ceiling-overflow-detour/climb-off-detour/blocked decisions, detour clears the footprint, and the exhaustive-detour fixtures: a 1.5 km wall detoured wide, a sealed courtyard blocked, a courtyard threaded through its one 70 m side gap, an 8 m slit — narrower than 2×clearance — correctly blocked), drone flight tool 3D-only enable + contract/settings defaults (`data-flight-*`, climb switch + ceiling input + camera-follow toggle + waypoint-altitudes popover with per-point overrides (`data-flight-alts`, sloped-leg planner units), pure `chaseCamera` units, asserts offline), Singapore default viewport (`data-center-*` render-computed from the persisted viewpoint), in-place fullscreen (fixed root covers the viewport, Escape exits — no remount of the live view), coordinate readout (pointer-tracked `data-lat`/`data-lon` + clipboard copy — offline-safe, `toMap` needs no tiles), view bookmarks (save dialog, jump-back via goTo asserted through `data-center-lon`, reload persistence, delete), deep link. OSRM routing is always mocked with an **echo mock** (line through the requested coords); an in-page CDN probe gates the online-only checks (view ready, attribution/zoom UI, click-driven pins with persistence + clear-all, waypoint editing: A→B distance, insert by clicking the line, per-leg breakdown popover on the result chip (`data-route-legs`, mocked legs split the totals), remove by clicking a marker, drag a marker to move it (re-routes with the moved coordinate), undo of remove/insert/clear, saved routes (save/load/delete + reload persistence), measure widget mount/unmount, pan → stationary viewpoint capture → reload persistence, drone flight in 3D: click-planted drone + waypoints with ground-elevation sampling, path length, a bbox-driven Overpass mock making the plan climb the mocked 80 m building, play/pause/reset animation via `data-drone-t`, a manual pan auto-releasing the follow cam, saved flight plans (save dialog → clear → load restores waypoints/altitudes/settings via `data-saved-flights`, delete), the climb switch re-planning legs as detours and back, clear, and the 2D switch releasing the tool) — on a CDN-blocked network the suite runs its offline branch |
| `131-strike-chaser` | kamikaze chaser enemy: pure — waves 1–2 are all-orbiter (the wave-1 `driftAmp >= 4` envelope invariant holds), from `CHASER_FROM_WAVE` (3) the LAST enemy of each wave is the one `variant === 1` chaser (index-derived, no extra rand draws) paying `CHASER_POINTS` 35 > 25 with difficulty-scaled hp, and `chaseMult` orders easy < normal < hard; behaviour through the REAL `stepEnemy` — a chaser lurks until the player is inside `CHASER_RANGE` then locks and closes to `< CHASER_CONTACT_R` never exceeding the per-step speed cap and never firing, a pad-safe player (`canChase=false`) freezes it in a hover (no teleport back to its ring), a building between chaser and player makes it CLIMB over (per-step `boomClipT` clip; its centre never enters the collider volume) and still reach the player, and a variant-0 orbiter stays exactly on its ring; live — wave 1 goes active and fields the seeded target count |
| `132-strike-jets` | jet-trooper flying gunners: pure — wave 1 is jet-free (`JET_WAVE` 2), the count ramps `min(1+⌊(w−2)/3⌋, 2, enemyCap)` with difficulty clamping, every jet is seeded airborne (y > 3, `driftAmp >= 3`, horizontal `driftAxis`, hp by difficulty, `POINTS.jet` 30), the gallery trim is pinned (`min(2+wave, 6)`, wave 1 still fields BOTH ringDrone and balloon), and the jet block sits after the difficulty-independent truck/car draws (the 108 byte-equality rule); the hover-strafe runs through the REAL `stepDrift` (displaces only its own horizontal axis, never leaves the validated envelope); the beam fires through the REAL `stepTurret` (hold-fire never shoots, a cleared jet spawns a `JET_BEAM` bolt from a raised muzzle, arms `fireTimer` for the model's flash pose, latches the stagger cooldown). From `STRAFER_FROM_WAVE` (4) the LAST jet is the one `variant === 1` **strafing run**: a long lane (`driftAmp >= 16`) above every roof, in bounds, paying `JET_STRAFER_POINTS` 40 > 30, while hoverers keep their short validated envelopes; through the REAL `stepDrift` a strafer streaks ≥ 8 u/s mid-pass, stalls < 1 u/s into the turnaround, spans the full run and never leaves its lane. Live — wave 1 boots jet-free with the seeded count, the pilot clears it, and the live wave-2 pool fields the seeded jet trooper |
| `133-strike-combo` | combo scoring (kill chain): pure — a fresh state has no chain, the first kill pays ×1 and opens the `COMBO_WINDOW` (5 s), a second inside pays ×2, the chain climbs to the ×`COMBO_MAX` (4) cap and holds, surviving inside the window but dying when it expires (`stepCombo`), restarting at ×1 after, breaking immediately on damage (`resetCombo`) and clearing with `resetCombatState`; live — `data-combo` is 0 at boot, one pilot kill opens a ×1 chain whose paid score equals a base (unmultiplied) seeded kind value, and the chain returns to 0 after `COMBO_WINDOW` of silence |
| `134-strike-shield` | shielded drone (only hurt from behind): pure — waves 1–4 field no `variant === 2` enemy and keep the normal 0.6 hit sphere (the seeded stream below `SHIELD_FROM_WAVE` 5 is untouched), from wave 5 exactly ONE shielded drone leads the enemy list (FIRST, the chaser stays LAST — index-derived, no extra rand draws) paying `SHIELD_POINTS` 45 > 35 on an honestly bigger `SHIELD_RADIUS` 0.8 airframe with difficulty-scaled hp; the `shieldBlocks` facing gate — a frontal shot (against the heading) deflects, a rear shot (with it) passes, side-on and top-down deflect, the `SHIELD_REAR_COS` boundary is exact, and a stalled drone's shield falls open; integration through the REAL `stepProjectiles` — bolts fired from ahead vs behind both connect, the HitEvent carries the normalized shot direction, and the gate deflects the frontal hit while damaging the rear one; live — wave 1 goes active and `data-deflects` boots at 0 |
| `135-strike-boss` | boss wave every 5th: pure — `isBossWave` marks 5/10/15/20 and nothing else, non-boss waves field no boss, boss waves exactly one, and it is the **LAST** target of the wave (the append-only stream rule) — with wave 5's other facts still holding (shielded drone first, chaser last, jets on their skyline lanes, crate still picked), proving no placement moved; the boss spec (hull sphere = `BOSS_RADIUS` = `BOSS_HULL_R`, aggregate `hp` = pods × `bossPodHp`, `hpMax`, `BOSS_POINTS` 150 above every other bounty, a slow wide orbit inside the world and clear of spawn, above every roof, pods toughening per boss and softer on Easy); the pool loads pods live with `hpMax` set while non-boss slots load with dead pods; `stepDrift` never moves the boss (`stepEnemy` owns its orbit). Pod geometry — pods ride the ring at hull height, evenly spaced, rotating with the phase; `podHitAt` counts a hull hit outward of a live pod, rejects bare armour between pods and the hull top, and rejects a destroyed pod's socket; `nearestLivePod` picks the pod facing the shooter, skips dead ones and reports −1 when all are gone; `podsLeft`. Integration through the REAL `stepProjectiles` — a bolt on a live pod's bearing resolves to that pod (damage) while one fired between pods resolves to armour (deflect), and draining every pod takes exactly pods × podHp hits, landing the aggregate hp on 0. Live — wave 1 boots non-boss (`data-boss-wave="no"`, `data-boss-active="no"`, zeroed boss telemetry, no health bar mounted) |
| `140-model-viewer` | Model Viewer widget: adding it lazy-loads the shared three.js chunk and renders a WebGL canvas, default model is the LEGO SWAT truck (`data-model`), the render loop advances the tick-owned `data-frames` counter (frame production proven without pixels), Animate and Auto-rotate toggles drive `data-animate`/`data-autorotate`, the picker switches between all catalog models (SWAT truck / AA turret / military truck — `data-model`, canvas and `data-frames` survive the swap), and both toggle settings + the model choice persist across reload |
| `141-console` | In-app console viewer (`docs/console.md`): pure — `formatArgs` prints top-level strings bare and nested ones quoted, renders Errors as `name: message`, guards cycles (`[Circular]`), caps depth (`{...}`) and truncates at `TEXT_LIMIT`; the ring appends in order, collapses CONSECUTIVE identical messages into one row with a climbing `count` and the newest time (a different level does not collapse), caps at `LOG_LIMIT` evicting oldest-first with monotonic ids, hands out an identity-stable snapshot, counts warn+error as issues, and clears both; `filterEntries` level buckets (incl. `issues` = warn+error) compose with a case-insensitive substring query; `formatEntries` exports one line per entry with level + `(xN)` repeats. Live — the app-bar button opens the dialog, a page-context `console.log` is captured with its level while leaving the badge alone, warn/error stream in with the dialog open and raise `data-issues`, object arguments render as fields (not `[object Object]`), five identical logs occupy ONE row (`data-repeat="5"`), an uncaught `setTimeout` throw and an unhandled promise rejection are both captured as errors (the whole point on a phone), the level filter publishes `data-filter` and shows that level only with `data-count` matching the rows, the text filter narrows to one, the buffer survives a route change but NOT a reload (session-scoped by design), Clear empties the list + badge + shows the empty state, and on a 390×844 phone the dialog goes full-screen with the message inside the viewport |

| `142-chunk-recovery` | Lazy-chunk recovery policy (`utils/lazyWithReload`, `docs/map.md`): a TRANSIENT chunk failure (first request aborted, then allowed) self-heals with exactly ONE reload and no error card, re-requesting the chunk in the fresh document — the reload's DOCUMENT request carries the `_rv` cache-buster (a plain reload can be served from a `max-age=600` index.html) and the parameter is stripped from the address bar while the `/map` route is preserved; a PERSISTENT failure shows the boundary's Reload card after exactly one reload and then stops (no loop over 3 s), with the latch recorded as `chunk-reload:<chunk>:<build>` holding a timestamp, and the card's button recovering + clearing the latch once the chunk is reachable; a latch seeded from an OLDER build neither suppresses recovery nor survives it (swept on write) — the bug that made a long-lived phone tab fail forever on a fresh deploy. Each case in its own context |
| `143-netplay` | Two-device play (`docs/netplay.md`): pure — the pairing codec against real Chrome SDPs (compact round-trip of ufrag/pwd/fingerprint/setup, mDNS `<uuid>.local` and IPv4 host candidates verbatim, the rebuilt data-channel m-line, an answer's `active` role, token under 300 chars vs ~700 of raw SDP), the raw `C0` escape hatch when the compact path can't represent an SDP (sha-512 fingerprint, no host candidates) and garbage-token rejection; the protocol validator (seat-by-role, move round-trip, and rejection of bad seats, negative/fractional plies and unknown enums). Live — two Connect 4 widgets in ONE document paired over the in-page `loopback` transport (`?netloop=1`, which swaps out only the networking): host publishes a code and reports `data-net="pairing"`, guest redeems it by typed code, both reach `connected` with host on `data-seat="toy"` and guest on `ninja`, the dialog closes itself, a guest tap out of turn is refused, a host drop relays (`data-ply`/`data-turn` agree on both), the host can't move twice, a guest drop relays back, a completed four leaves both on `data-winner="toy"`, New game from either side clears both boards, and leaving online mode releases the link. Then the dead-link UX: the PEER leaving closes the survivor's link (`data-net="closed"`), whose chip reads "Connection lost" (never "tap to connect"), whose dialog shows the `netplay-lost` alert with the stale token GONE (the RTCPeerConnection behind it is unusable), and whose `netplay-repair` button mints a fresh code back in `pairing` |
| `144-netplay-webrtc` | The same pairing through a REAL `RTCPeerConnection` — the end-to-end proof that a QR-sized token opens a data channel. Two devices are two separate browser CONTEXTS, not tabs: same-origin tabs share localStorage and the boards would agree through redux-persist rather than through the link. Offer (`C1o…`) and reply (`C1a…`) tokens both come out ~118 chars, the channel opens on both sides, seats assign by role, and a drop crosses the wire (`data-ply`/`data-turn` on the far context). Also the avatar COSTUME, provable only here because the contexts have separate localStorage: the host swaps Player 1 to DarkArin on its real Settings page, the guest renders the host's pick (`data-avatar-toy="darkarin"`) despite its own default settings. Then the guest's page closes OUTRIGHT while linked — the real-transport proof of the grace timer + heartbeat (`RECONNECT_GRACE_MS` 8s / `DEAD_MS` 16s): the host notices by itself (`data-net` reaches `failed`/`closed` with no close event guaranteed to cross), its chip reads lost and its dialog offers Pair again with the stale token gone; a fresh page in the guest's context then renders the guest's OWN avatars — proving the costume sync never wrote to its settings. Chromium runs with `--disable-features=WebRtcHideLocalIpsWithMdns` (`launch({ args })`) because a container has no mDNS responder for `.local` candidates; on real wifi that resolution is exactly what makes it work |
| `145-maze` | Maze Runner (`docs/maze-runner.md`): pure — determinism (same seed + dims → byte-identical walls, a different seed differs), the PERFECT-maze invariant over every size × orientation × 25 seeds (`cols*rows-1` passages, so no island and no cycle) plus BFS solvability, a sealed outer wall, walls always blocking and the one-cell rule moving exactly one cell, the junction rule never crossing a wall / never sliding past a junction, dead end or goal while genuinely running corridors (avg ~8.9 cells vs 1.0), the mirror used for player 2 (same passage count, same solution length, corners swapped, mirroring twice is the identity), `wallSegments` emitting each wall once with walls + passages accounting for every grid edge, `solve`/`pathToDirs` walking to the goal, and BFS fog respecting walls + depth. Live — defaults (Step is the default rule, mirror on), the default one-step rule moving exactly one cell, a wall refusing a move without starting the clock, switching to Run, a CDP swipe moving the runner, the trail recording cells; then **the suite rebuilds the widget's own maze from its published seed/dims, computes the real solution and walks it CLOSED-LOOP** (one Run-rule press eats several route cells, so an open-loop replay desyncs) to assert the win, the committed `data-ms`, the recorded best and the celebration; New maze re-seeds and resets; aid/rule switch live in solo and the one-step rule moves exactly one cell; maze + position + settings survive a reload; a mid-run size change is ConfirmDialog-guarded both ways. Mirror — `data-mirror` defaults on, turning it off mid-duel is ConfirmDialog-guarded and restarts, and with it off P2 starts from the SAME corner at the same goal instead of the reflection; the toggle is hidden in solo while the attribute stays published. Hot seat — P1 walks to the goal (watched via `data-turn`, since the hand-off fires in the same tick and `data-pos` never reads as P1's goal), the banner names P2, P2 starts on the MIRROR's corner, **window keys are inert behind the banner** and P2's clock has not started, then P2 finishes and the duel ends with a winner + best time |
| `146-ttt-online` | Tic-Tac-Toe over two devices — the netplay layer's SECOND consumer, and the check that `docs/netplay.md`'s "game-agnostic" claim is true rather than asserted: the widget supplies only an `applyMove` (put this seat's mark in this cell) and inherits seats, the turn lock, the relay, sync and restart from `features/netplay/useNetGame`. Two Tic-Tac-Toe widgets in one document over `?netloop=1` (`addTicTacToeWidgets` + the shared `pairLoopback` helper, which `143` now uses too): link off outside online mode, the host half-paired at `data-net="pairing"`, both reaching `connected` with seats by role, a guest tap out of turn refused, a CELL INDEX relaying both ways with `data-ply`/`data-turn` agreeing, the host unable to move twice, an occupied cell refused, a completed top row leaving both on `data-winner="toy"`, New game from either side clearing both boards and the win, and leaving online mode releasing the link |
| `147-maze-race` | Maze Runner's two-device ghost race (`docs/maze-runner.md`) — the netplay layer's first NON-turn-based consumer, sitting on `useNetplay` directly rather than `useNetGame`, and speaking `go`/`pos`/`done`. Two Maze Runner widgets in one document over `?netloop=1` (`addMazeWidgets` + the shared `pairLoopback`): seats by role, the host's `sync` carrying seed, dimensions AND move rule to the guest so both race the same maze, Start counting both devices down together (`data-race="counting"`, overlay present, keys inert), both unlocking together at the maze entrance, a swipe on ONE board relaying to the other's `data-ghost` and drawing a ghost marker, the closed-loop solution walk making that device `won` and the other `lost` with a recorded time, the loser still free to finish, and leaving the mode being confirm-guarded (Keep playing keeps it). Then the void path: a rematch is started and the OTHER device leaves mid-race, closing its link — the survivor's `data-race` becomes `void` (never `running` forever, the original field failure), the `maze-race-void` overlay names the lost connection, the stale ghost and Start race are gone (the button needs a connected link), the chip reads `closed`, and a voided race is over rather than in progress, so leaving the mode then needs no confirm. **Driven by swipes, not keys**: every maze's key handler listens on `window`, so an arrow key moves BOTH runners on the page and the ghost would sit exactly on the local marker |
| `148-archery-online` | Archery over two devices — the netplay layer's first REAL-VALUED move (`docs/archery.md`). Pure — `packShot`/`unpackShot` round-trips (velocities exact, phases within their 9ms step, `pack(unpack(x))` the identity, garbage rejected), seeded heights + per-shot wind deterministic, and `resolveShot`'s fixed-step determinism, with a scanner proving robust hits and misses exist. Live — two widgets over `?netloop=1` (`addArcheryWidgets` + `pairLoopback` + the `dragShot` CDP helper, which must `scrollIntoViewIfNeeded` first: CDP touches don't auto-scroll and a below-the-fold board silently swallows the drag): the guest adopts the host's dealt heights and seed, an out-of-turn drag is refused, then CLOSED-LOOP archery — the suite reads the live world off the DOM, scans a ±3-unit-robust hit with the bundled resolver, inverts it into the slingshot drag (−v/K) and fires: the shot relays as one packed integer, both devices agree on the score and turn; a scanned miss relays and scores nothing; a GUEST restart re-deals both devices identically via `sendSync`; leaving the mode releases the link; and one local pass-and-play check (same resolver, state lands with the arrow) gives Archery its first coverage. `data-arrow` gates every drag — a replay in flight refuses input |
| `149-othello` | Othello (`docs/othello.md`): pure, against the bundled `othelloModel` — the standard 4-disc opening with exactly the classic four legal moves, a capture reported with its flips and the source position untouched, multi-ray capture in one move, no-flip and occupied cells illegal, the FORCED PASS (a stranded opponent is skipped and the turn stays — the reason a position stores its own `turn`: passes break disc parity), the early wipe-out end with most-discs-wins, and the AI ladder: every difficulty opens legally, easy (the deterministic-under-seed "sane player") never misses an offered corner, seeded-easy loses to medium and medium loses to hard in fully deterministic playouts. Live — one widget: the opening publishes `data-legal="4"` with hints on exactly those cells, an illegal tap is refused, a real move lands the disc AND turns the captured one (`data-disc`, scores 4–1), a mid-game mode change is confirm-guarded, the computer replies on its own and the pass-opening button hands it the first move, and the position + mode survive a reload (after waiting out redux-persist's debounced write, lesson #12). Press-hold capture preview: holding a legal cell publishes `data-preview` with a ghost disc and rings (`data-preview-flip`) on exactly the model's `flipsFor` set while the position stays untouched, releasing on the cell commits the move (the ordinary click) and clears the preview, and dragging off the cell before release cancels both. Flip cascade: closed-loop play until a distance-2 capture arises, then the adjacent captured disc carries `data-flip-delay="0"` while the distant one waits its ring (≥70ms) and untouched discs carry none |
| `150-othello-online` | Othello over two devices — the netplay layer's fourth turn-based consumer, and the proof `useNetGame` stretches to a game whose turn is NOT parity: the `TBoard` is a whole position `{ cells, turn }`, and a forced pass never crosses the wire (both devices derive it in the same pure `applyMove`; the pass itself is proven in 149's rules half). Two widgets over `?netloop=1` (`addOthelloWidgets` + `pairLoopback`), moves chosen CLOSED-LOOP by replaying the game through the bundled model rather than hardcoding a script: seats by role, the guest dead out of turn, a cell relaying with the CAPTURE visible on the far board and both scoreboards agreeing, the host refused on the ninja's turn, a no-flip cell refused across the link, six closed-loop plies leaving both boards and scores in step, a broadcast New game restoring the standard opening on both, and leaving the mode releasing the link |

Drone Strike suites steer with the same closed-loop rig (`createStrikePilot`)
aimed at the HUD's nearest-target beacon (`data-tgt-*`); `engage()` fires via
keyboard Space so the trigger never disturbs the sticks' touch ownership.
Tank Battle suites use `createTankPilot` (left stick drives the hull, right
stick steers the camera aim); its `engage()` drives INTO terrain line of
sight first — over the contour there is usually no lock until the pilot
crests the ridge — then fires on lock + ballistic solution.

Routes must respect the game rules: laps only start under the drone's own
power, and fast legs cruise **above** the skyline (24 — waypoint tolerance
can start a leg ~2 low) because crash mode (on by default) punishes
full-speed flight at building height.

**Flake policy**: the pilot's precision maneuvers (threading a 2-unit ring,
touching down on a 1.6-unit pad, timed wall hits) carry a small miss rate
under software-GL load, which compounds across a full run. The runner
re-runs a failed suite once with a fresh browser and logs the retry — a
suite that fails twice is a real failure.
