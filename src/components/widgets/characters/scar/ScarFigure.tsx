// Scar's static full-body figure — SMG at low ready, canister in hand.
import SoldierFigure from './SoldierFigure'

export default function ScarFigure() {
  return <SoldierFigure assaulting={false} />
}
