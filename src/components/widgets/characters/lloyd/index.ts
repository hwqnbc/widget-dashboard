// The Lloyd avatar's grouped pieces: head (chip), full figure, and the
// looping sword-chop celebration, plus its palette. The avatar registry
// assembles the { Head, Figure, Celebration } bundle from these.
// Lloyd is 2D-only for now — the 3D model ships in a later round (the
// Avatar Actions 3D view shows the "no 3D figure yet" placeholder).
export { default as LloydHead } from './LloydHead'
export { default as LloydFigure } from './LloydFigure'
export { default as LloydCelebration } from './LloydCelebration'
export { LL } from './lloydPalette'
